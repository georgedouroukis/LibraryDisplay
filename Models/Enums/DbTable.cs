namespace LibraryDisplay.Models.Enums
{
    /// <summary>
    /// used for ClickableLabels, ClickableBookItems to identify where they belong and EditStatus to identify where we are 
    /// </summary>
    public enum DbTable
    {
        Book = 1,
        Author = 2,
        Publisher = 3,
        Genre = 4
    }
}
